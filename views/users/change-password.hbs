<div class="users-container">
	<h1 class="page-title">Alterar Senha do Usuário</h1>

	{{#if message}}
		<div class="alert alert-danger">{{message}}</div>
	{{/if}}

	<form method="POST" action="/users/{{user._id}}/change-password" class="user-form">
		<div class="form-group">
			<label>Usuário:</label>
			<input type="text" value="{{user.name}}" disabled>
		</div>

		<div class="form-group">
			<label>Email:</label>
			<input type="email" value="{{user.email}}" disabled>
		</div>

		<div class="form-group">
			<label for="newPassword">Nova Senha:</label>
			<input type="password" id="newPassword" name="newPassword" minlength="6" required>
		</div>

		<div class="form-group">
			<label for="passwordConfirm">Confirmar Nova Senha:</label>
			<input type="password" id="passwordConfirm" name="passwordConfirm" minlength="6" required>
		</div>

		<div class="form-actions">
			<button type="submit" class="btn-primary">Salvar Nova Senha</button>
			<a href="/users/{{user._id}}/edit" class="btn-secondary">Voltar</a>
		</div>
	</form>
</div>
